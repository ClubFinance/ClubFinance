{% extends 'base.html.twig' %}

{% block pagetitle %}Benutzerverwaltung{% endblock %}

{% block body %}
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h6 class="m-0 font-weight-bold text-primary">Benutzer "{{ user.vorname }}{{ user.nachname }}" bearbeiten</h6>
    </div>
    <div class="card-body">
        <a href="{{ path('users') }}" class="btn btn-info btn-icon-split mb-3">
            <span class="icon text-white-50">
                <i class="fas fa-undo-alt"></i>
            </span>
            <span class="text">Zurück</span>
        </a>
        {{ form_start(form) }}
        {{ form_widget(form) }}
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}